#include <stdio.h>
#include <stdlib.h>

/**
 * main - main block
 * Description: program that prints the alphabet in lowercase
 *
 * Return: Always 0
 */

int main(void)
{
	char num = 'a';
	
	while (num <= 'z')
	{
		putchar(num);
		nu++;
	}
	putchar('\n');
	return (0);
}
